- Start adding some tests (parsing and scanning ok/ko)
- Implement Pratt Parsing
- Add function calls
- Add window functions
- Add suite of tests (ok/ko parsing)
- We have a vec in from exprs (we dont need it)
- Fix parsing for table names without quoted identifiers (e.g., project-id.dataset-id.table-id)
- Start implementing Lineage
	- Implement lineage for select * except (col1, col2...)
	
